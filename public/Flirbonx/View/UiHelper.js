/**
 * Le UiHelper aide à la mise en forme des données, en appelant des fonctions 
 * secondaires depuis les fichiers html de partials
 */
export class UiHelper {
    constructor(uiRenderer) {
        this.uiRenderer = uiRenderer;
    }
}