import { UiController } from "../UiController.js";

export class FriendsListController extends UiController {
    constructor(uiManager) {
        const domElements = {};
        super(uiManager, domElements);
    }
}